import 'package:flutter/material.dart';

// Definição das cores da paleta
const Color laranjaVivo = Color(0xFFFF6F00);
const Color laranjaSuave = Color(0xFFFFA040);
const Color pessegoClaro = Color(0xFFFEE0B2);
const Color cinzaEscuro = Color(0xFF333333);
const Color cinzaClaro = Color(0xFFF5F5F5);
const Color branco = Color(0xFFFFFFFF);

// Novas cores para um design mais profissional
const Color primaryColor = laranjaVivo; // Sua cor principal
const Color accentColor = Color(0xFF4CAF50); // Um verde para sucesso, por exemplo
const Color textColorPrimary = Color(0xFF212121); // Texto escuro principal
const Color textColorSecondary = Color(0xFF757575); // Texto secundário/detalhes
const Color backgroundColor = Color(0xFFF5F5F5); // Fundo claro para seções
const Color cardColor = Colors.white; // Cor de fundo dos cards
const Color borderColor = Color(0xFFE0E0E0); // Cor para bordas sutis
const Color errorColor = Colors.red; // Cor para erros

// Adicione mais tons se necessário para gradientes ou estados
const Color laranjaClaro = Color(0xFFFFB74D);
const Color laranjaEscuro = Color(0xFFE65100);